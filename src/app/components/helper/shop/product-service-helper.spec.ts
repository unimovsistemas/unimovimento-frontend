import { ProductServiceHelper } from './product-service-helper';

describe('ProductServiceHelper', () => {
  it('should create an instance', () => {
    expect(new ProductServiceHelper()).toBeTruthy();
  });
});
